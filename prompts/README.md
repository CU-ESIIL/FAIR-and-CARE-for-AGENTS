# Consequential instructions

`PROMPT_LOG.md` is the append-only request history for this public project. Before consequential work, use `templates/agent-task.md` to define **Goal → Instructions → Test → Record**. After the work, create a structured run record in `provenance/records/`.

Do not reproduce secrets, personal information, governed knowledge, sensitive ecological locations, or security details in the public log. Use a typed marker such as `[REDACTED: credential]` and follow the authorized private process. This safety exception takes precedence over verbatim public logging; the omission and reason should remain explicit without exposing the content.
